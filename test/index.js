const assert = require('assert');
const should = require('chai').should();


describe('Repeater#isConnected()', function() {
    it('should return false when the connect not called yet', function() {
        assert.equal(false, false);
    });
});
